---
title: "Diagonal-Root Numerical Procedure"
subtitle: "How does root_hint initialize one reproducible loaded-root procedure?"
toc-depth: 2
---

**Status:** existing diagonal-root procedure plus `CONVERGING` selected-View
element-root extension. The physical quantities are defined by the
[Runtime Contract](../v1-runtime-contract.qmd#typed-direct-quantity-catalog).

## Solve one scalar Schur residual

For the `exp(-i omega t)` convention, the complete loaded Direct operator is

$$
Q(\omega)=K-\omega^2C-i\omega G
-i\omega\sum_s B_s Z_s(\omega)^{-1}B_s^T,
\qquad Z_s(\omega)=R_s-i\omega L_s.
$$

Here $G$ is the effective node conductance for the View. It contains
the intrinsic primitive conductance and every logical-Port matched load left by
the PTC mask; the separate sum retains each series-RL branch:

$$
G=G_{\mathrm{intrinsic}}+B_pD_p^{-1}MD_p^{-1}B_p^T.
$$

A terminal `retain()` of an unported node changes only the
zero-external-current partition; it neither promotes that node to a Port nor
removes an existing Port load. Consequently the loaded linewidth can be set by
a 50-ohm Port attached elsewhere in the eliminated complement.

The final View owns one ordered terminal basis $R$. A diagonal root selects
$i=j$ in that basis; an `OperatorElementRootSpec` selects ordered row $i$ and
column $j$. Neither selection changes $R$ or requires the full capacitance
matrix to be positive definite. An unsolvable eliminated block fails closed;
there is no pseudoinverse or descriptor-mode fallback.

With $E$ the eliminated complement, use checked solves

$$
X=Q_{EE}^{-1}Q_{ER},\qquad
g_{ij}=Q_{ij}-Q_{iE}X_{:j}.
$$

The analytic derivative includes the same series-RL branches:

$$
Q'=-2\omega C-iG+
\sum_s B_s\left[-iZ_s^{-1}+\omega Z_s^{-1}L_sZ_s^{-1}\right]B_s^T.
$$

Using this $Q'$, solve

$$
X'=Q_{EE}^{-1}(Q'_{ER}-Q'_{EE}X)
$$

and evaluate

$$
g'_{ij}=Q'_{ij}-Q'_{iE}X_{:j}-Q_{iE}X'_{:j}.
$$

The baseline starts at real $\omega_0=2\pi\,\texttt{root_hint}$ and performs
at most 32 analytic complex-Newton updates

$$
\omega_{k+1}=\omega_k-\frac{g_{ij}(\omega_k)}{g'_{ij}(\omega_k)}.
$$

`root_hint` is only this deterministic initial locator. SCNSim does not sweep
for a minimum, enumerate the QEP spectrum, or replace the converged root with a
nearer eigenvalue.

## Require a machine-resolution certificate

Let $n$ be the complete operator dimension,
$\epsilon=\operatorname{eps}(\mathrm{Float64})$, and

$$
\tau(n)=256(n+1)\epsilon.
$$

Define $x_j=1$, $x_E=-X_{:j}$ and zero on the other retained
coordinates. Only the selected row $i$ and eliminated rows are root-equation
rows; the other retained rows belong to the complete View but need not be
null. Define

$$
A_{\rm abs}=|K|+|\omega||G|+|\omega|^2|C|
+\sum_s\left|-i\omega B_s Z_s(\omega)^{-1}B_s^T\right|.
$$

The Result is materialized only when all values are finite, $Q_{EE}$ is
solvable, $\operatorname{Re}\omega>0$, and
all four normalized errors are at most $\tau(n)$:

$$
\eta_E=
\frac{\|Q_{EE}X-Q_{ER}\|_\infty}
{\||Q_{EE}||X|+|Q_{ER}|\|_\infty},
\qquad
\eta_Q=
\frac{\|[Q(\omega)x]_{\{i\}\cup E}\|_\infty}
{\|[A_{\rm abs}|x|]_{\{i\}\cup E}\|_\infty},
$$

$$
\eta_f=\frac{|g_{ij}|}{[A_{\rm abs}|x|]_i},
\qquad
\rho_\omega=\frac{|g_{ij}/g'_{ij}|}{|\omega|}.
$$

When $E$ is empty, its solve is a no-op and $\eta_E=0$. For a nonempty but
exactly decoupled complement, the $0/0$ eliminated-block ratio is likewise
defined as zero; a zero denominator with nonzero numerator is infinity and
fails the certificate. Other zero normalization denominators fail rather than
being replaced by an arbitrary floor.

With

$$
s_{g'}=|Q'_{ij}|+|Q'_{iE}||X_{:j}|+|Q_{iE}||X'_{:j}|,
$$

the normalized slope must also satisfy $|g'_{ij}|/s_{g'}>\tau(n)$. The checked
rows of $Q(\omega)x$ are recomputed from C/G/K and series-RL branches rather
than reconstructed from the Schur residual. The generic element root has no imaginary-sign policy;
the diagonal consumer separately requires $\operatorname{Im}\omega\le0$,
including when it reuses a numerical root. These normalized certificate values
and any conditioning
diagnostics are execution-time checks, not durable Result or receipt fields.
The public Result retains the physical local `slope` $g'_{ij}$; its verified success
binds its exact algorithm ID and semantic runtime source digest
that performed the complete certificate. SCNSim adds no separate
design-quality threshold.

The normalized nonzero slope is machine-resolution evidence consistent with a
simple, locally isolated root. It is not a validated complex enclosure or a
proof of global uniqueness. V1 makes no claim that it found the root nearest to
`root_hint`; the hint selects the deterministic Newton basin only.

Bitwise stagnation means that the proposed update leaves both the real and
imaginary Float64 bit patterns of $\omega$ unchanged. It is success only if
this certificate passes. Otherwise the request returns
`NumericalResolutionUnresolved`,
`EliminatedBlockSolveFailure`, or `RootSlopeUnresolved`. A missing, non-finite,
non-positive, or non-frequency `root_hint` returns `InvalidDiagonalRootHint`.
The full capacitance matrix is not Cholesky-gated; a singular eliminated solve
still fails explicitly.

## Continue every ParameterSet from the baseline

For a non-baseline ParameterSet, bind physical parameters in canonical SI and
follow

$$
p(t)=p_0+t(p_1-p_0),\qquad 0\le t\le1.
$$

Every candidate begins again at the sealed baseline root. Newton first attempts
the endpoint of the current interval. A numerical nonconvergence bisects that
interval and retries from its certified left endpoint, to a maximum depth of
32. After a midpoint succeeds, continuation resumes toward the original right
endpoint. It never uses the preceding CMA-ES candidate, reapplies `root_hint`,
or takes an automatic detour.

The baseline anchor and every continuation point realize the same View named
by the request before evaluating the diagonal root. Reduction, PTC, transform,
and retain therefore remain part of the quantity's numerical lineage; neither
the baseline nor an intermediate point falls back to the raw compiled Plan.

A repeated/unresolved slope, eliminated-block singularity, mapped physical
domain failure, or exhausted subdivision returns a typed non-continuable
failure. All constants, formulas, branching rules, and failure mappings are
part of the runtime algorithm ID.

This procedure fixes a reproducible baseline-owned numerical lineage, not a
validated enclosure of the entire analytic branch. A collision exposed by an
unresolved slope or failed continuation is rejected, but V1 does not claim to
detect every possible Newton-basin jump between certified endpoints. A global
branch proof would require a later enclosure-based algorithm and a new runtime
algorithm identity; complete-QEP nearest-root matching is not a substitute.

[Next: Direct poles, zeros, couplings, and operators](direct-poles-zeros-and-operators.qmd)
